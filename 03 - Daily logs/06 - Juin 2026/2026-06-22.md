# 📅 Lundi 22 Juin 2026 — Fix ajout au panier (fiche catalogue)

## 🧠 Humeur & contexte

Reprise après le week-end. Scope planifié vendredi 19 : le bouton « Ajouter au panier » sur la fiche produit catalogue est visible mais le clic ne produit aucun effet (pas de toast, dropcart vide). Diagnostic posé — crash silencieux dans le hook formulaire produit.

**Symptôme :** clic sans effet sur `/produit/{sku}`.

**Scope confirmé :** panier Redux local uniquement (toast + dropcart + page `/panier`). Pas d'API ERP, pas de bouton « Passer commande ».

**Cause racine probable :** `data.options` undefined au submit dans le hook formulaire — `Object.keys(undefined)` lève une exception avant le dispatch Redux.

---

## 🎯 Objectifs du jour

- [ ] Fix hook formulaire produit : `options: {}` en defaultValues + guard `data.options ?? {}` + parsing quantité robuste
- [ ] Fix collatéral : `.catch()` sur le hook async action pour éviter le bouton bloqué en loading
- [ ] Sanity check adaptateur produit (prix > 0 sur fiche ERP)
- [ ] Validation manuelle : toast, compteur header, dropcart, `/panier`, incrément quantité, régression quickview
- [ ] `bun run build` front

---

## 💡 Découverte du jour

Pas de découverte particulière.

---

## ✅ Réalisations

### Scope — fix ajout panier Redux
*Réparer le clic « Ajouter au panier » sur la fiche catalogue.*

- À compléter

---

## 🧠 Ce que j'ai appris

- À compléter en fin de journée.

---

## ⚠️ Points de vigilance

- Hors scope : API ERP panier, checkout `/panier` (pas de bouton submit aujourd'hui)
- Si bouton absent (pas visible) sur certains SKU : vérifier la règle stock `out-of-stock` sur la fiche — cas distinct du bug du clic
- Produits fake homepage → quickview inchangé (même hook — tester en régression)

---

## ⏭️ Next steps

- Dès déblocage Patrick : re-smoke PIM, bascule source catalogue PIM, Slice B homepage
- Backlog : pièces catalogue commandables sans stock physique (`is_catalog_part`)
- Backlog : bouton « Passer commande » sur `/panier`

---

## ⚡ Énergie

-

---

## Référence technique (plan Cursor)

**Fichiers cibles :**
- Hook formulaire produit (defaultValues + guard options)
- Hook async action (catch loading)

**Commit attendu :** `fix/frontend : 'fix add to cart form options crash on catalog PDP'`

**Checklist validation :**
1. `/produit/{sku}` → clic → toast + dropcart
2. `/panier` → ligne produit visible
3. Re-clic → quantité incrémentée
4. Quickview homepage → pas de régression
