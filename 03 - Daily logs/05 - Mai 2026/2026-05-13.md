# 🗓️ Mercredi 13 Mai 2026 - Mise en place d'un plan B pour l'API

## 🧠 Humeur & contexte

La journée s'annonce stratégique. PartSmart tarde à répondre — on ne les attend plus. Patrick et moi avons tranché : on va construire notre propre solution pour fetcher la liste des modèles. Je me sens responsable du chantier, sans pression particulière.

## 🎯 Objectifs

- Trouver une alternative à l'iframe PartSmart
- Construire les endpoints de recherche côté Go
- Implémenter l'UI de recherche native Midbec

## 💡 Découverte du jour

En inspectant les requêtes réseau de l'iframe PartSmart via DevTools, j'ai découvert que PartSmart expose une API interne non documentée accessible avec notre Bearer token existant. Les endpoints clés :

- `GET /api/search/en/models?query=...` — recherche de modèles
- `GET /api/search/en/parts?query=...` — recherche de pièces
- `GET /api/search/en/model/{catalogId}/{modelGuid}` — détail modèle + sections IPL + images CDN
- `GET /api/search/en/ipl/{catalogId}/{iplGuid}?modelId=...` — vue explosée + hotspots

La réponse IPL contient les coordonnées x/y/w/h de chaque pièce sur le diagramme — ce qui permet de construire une vue explosée interactive 100% native, sans iframe.

## ✅ Réalisations

### Scope 8 — API interne PartSmart découverte
*Reverse engineering de l'iframe via DevTools Network tab*
- Identification de 5 endpoints internes PartSmart via interception des requêtes XHR
- Validation de tous les endpoints via Postman — données structurées confirmées
- Catalogues Midbec identifiés : `fri` (Electrolux/Frigidaire), `whtst` (Whirlpool Legacy)

### Scope 8 — Backend Go : endpoints de recherche
*Extension du client PartSmart + 4 nouveaux handlers*
- Ajout de `SearchModels`, `SearchParts`, `GetModel`, `GetIPL` dans `internal/clients/partsmart/client.go`
- Création de `internal/httpserver/handlers/partsmart_search.go` — 4 handlers publics
- Routes enregistrées dans `router.go` : `/partsmart/search/models`, `/partsmart/search/parts`, `/partsmart/model/{catalogId}/{modelGuid}`, `/partsmart/ipl/{catalogId}/{iplGuid}`
- Build propre, tous les endpoints validés en Postman ✅
- Branche `feat/partsmart-search-api` pushée sur `midbec-go-api`

### Scope 8 — Frontend Next.js : UI de recherche native
*Remplacement complet de l'iframe par une UI React native*
- Création de `_components/ModelSearch.tsx` — recherche par modèle avec autocomplétion (debounce 300ms), filtre fabricant par `catalogCode`
- Création de `_components/PartSearch.tsx` — recherche par numéro ou nom de pièce
- Création de `_components/types.ts` — types et fonctions de normalisation partagés
- Création de `[catalogId]/[modelGuid]/page.tsx` — page détail modèle avec dropdown IPL + vue explosée interactive (hotspots cliquables, highlight bidirectionnel image ↔ tableau)
- Fix `parseModelsResponse` — extraction correcte des items depuis les groupes de réponse API
- Fix filtre fabricant — mapping `catalogCode` côté frontend au lieu d'injecter le fabricant dans le query param
- Patrick impressionné par la découverte de l'API interne ✅
- Branche `feat/partsmart-search-ui` pushée sur `midbec-front`

## 🧠 Ce que j'ai appris

**Reverse engineering d'une iframe** : quand un outil tiers n'expose pas d'API documentée, inspecter les requêtes réseau de son iframe dans DevTools révèle souvent une API interne consommable. C'est une technique de senior — chercher avant de conclure qu'il n'y a rien.

**Chaining d'API** : les endpoints PartSmart sont conçus en cascade — chaque réponse fournit les identifiants nécessaires pour l'appel suivant. Il faut comprendre le flow complet avant de construire quoi que ce soit.

**Filtrage côté frontend vs query param** : quand une API ne supporte pas le filtrage par un critère métier, le filtre s'applique sur les résultats reçus, pas dans la requête. Passer le fabricant dans le query param PartSmart donnait des résultats vides.

## ⚠️ Points de vigilance

- `scan@midbec.com` toujours en attente de passage en Admin — credentials `rpina@midbec.com` utilisés temporairement en dev
- Le token PartSmart est regénéré à chaque requête (2 appels réseau supplémentaires) — à cacher en mémoire côté Go avant la mise en prod
- L'API interne PartSmart n'est pas documentée — elle peut changer sans préavis
- Fix UI restants pour la semaine : suppression description dans dropdown, page détail à styler, comportement dropdown IPL

## ⏭️ Next steps

- Fix UI : supprimer description dans les résultats de recherche
- Styler la page détail modèle `/recherche-par-modele/[catalogId]/[modelGuid]`
- Tester le flow complet (recherche → clic → page détail → sélection plan → vue explosée)
- Ouvrir les MRs `feat/partsmart-search-api` et `feat/partsmart-search-ui` vers `develop`
- Mettre à jour Notion Scope 8

## ⚡ Énergie

Journée exceptionnelle — la découverte de l'API interne PartSmart a tout changé. Partir d'un plan B incertain (Playwright + scraping) pour arriver à une intégration propre et performante en une journée, c'est satisfaisant. Patrick était impressionné — bonne dynamique pour la suite.