# 🗓️ Lundi 04 mai 2026 - Scope 2 : Chantier Tailwind Migration (suite)

## 🧠 Humeur & contexte

Journée pluvieuse mais journée d'audit. Le nettoyage continue.

## 🎯 Objectifs

- Passer en revue l'état du Scope 2
- Identifier et supprimer le dead code SCSS header
- Préparer le chantier search pour mardi

## 💡 Découverte du jour

`data-midbec-header` n'est pas du legacy à supprimer — c'est le système de remplacement de `site--desktop-header--standard-one` introduit lors d'une session précédente. Le grid complet du header vit dans `tailwind.css` sous `[data-midbec-header="standard"]`. Le fichier `_header.scss` était vide depuis le début.

## ✅ Réalisations

### Scope 2 — Dead code header

_Audit complet du bundle SCSS header avant toute action_

- Audit grep `header__`, `topbar__`, `logo__`, `main-menu__`, `megamenu__`, `menu__`, `phone__` — zéro consommateur TSX confirmé
- Suppression de 8 fichiers SCSS header (`_header`, `_logo`, `_topbar`, `_main-menu`, `_megamenu`, `_megamenu-links`, `_menu`, `_phone`) — **-781 lignes**
- Régression search détectée post-suppression — `_search--header--standard.scss` contenait des custom properties CSS actives (`--search-ship-*`) et des couleurs scheme non migrées
- Restauration de `_search--header--standard.scss` + `mixins/_header.scss` + import `layout.tsx` — régression corrigée
- Commit : `chore(scss): remove header SCSS dead code, -781 lines`

## 🧠 Ce que j'ai appris

**Audit exhaustif par fichier** : vérifier chaque namespace BEM individuellement avant suppression. `search__` n'avait pas été couvert — une vérification manquante suffit à créer une régression.

**Un fichier SCSS peut être partiellement migré** : la géométrie dans `tailwind.css`, les tokens calculés encore en SCSS. Supprimer sans vérifier les `--custom-properties` = régression silencieuse sur les couleurs.

**"If it ain't broke, don't fix it"** : le revert n'est pas un argument architectural — Git protège dans les deux cas. La vraie question est de réduire le risque de régression, pas de le fragmenter.

## ⚠️ Vigilance

- `_search--header--standard.scss` : migration incomplète — les couleurs du scheme input/button sont encore calculées depuis les variables SCSS. Ne pas supprimer avant migration complète mardi.
- Toujours grep chaque namespace BEM avant suppression, sans exception.

## ⏭️ Next steps

- **Mardi 6 mai — journée complète** : migration `_search--header--standard.scss` → tokens CSS dans `tailwind.css`, suppression du fichier et de `mixins/_header.scss`
- Suite : `_typography.scss`, `_btn.scss`, `_card.scss` (à évaluer)
- Suite : chantier shop (`_products-list`, `_checkout`, `_product`, `_product-form`, `_review`)

## ⚡ Énergie

Journée méthodique et honnête — une régression attrapée à temps, un réflexe senior bien ancré.
