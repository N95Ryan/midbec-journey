# 📅 Mercredi 25 mars 2026 — Impression de déjà-vu et header bouclé

Énorme impression de déjà-vu aujourd'hui — de l'écran aux lignes de code.
J'ai l'impression d'avoir déjà vécu exactement cette situation.

Au programme : clôturer la partie header / contact côté produit, attaquer
le body, et trancher des questions d'architecture qui traînaient.

**Découverte du jour :** le header React n'embarque plus Bootstrap en JSX,
mais le Sass tire encore les mixins / variables Bootstrap pour compiler —
ce n'est pas la même dette que les classes `d-md-none`.

---

## ✅ Réalisations du jour

### Header / Contact (commit `aec8485`)
- **Topbar noire** : dropdown succursale remplacé par un lien
  « Succursales » (i18n) vers `/contact`, positionné avant le
  sélecteur de langue (`Topbar.tsx`)
- **Page `/contact`** : remplacement du stub par un contenu structuré
  (`ContactPageContent.tsx`)
  - Bloc *Pour nous joindre* : courriel, sans frais
    (`MIDBEC_PUBLIC_PHONE`), fax (`midbecSite.ts`)
  - Section *Succursales* : 8 cartes alignées sur `headerBranches.ts`,
    adresses via clés `BRANCH_CONTACT_ADDRESS_*`,
    lien Coordonnées → `midbec.com/succursale/{slug}` (`branchContact.ts`)
- **i18n** : clés contact + adresses en fr / en / ru ;
  `NAV_CONTACT` ajouté en russe ; libellé topbar ajusté
- **Nettoyage** : suppression de `DropdownBranch.tsx` et
  `usePreferredBranch.ts` (plus de préférence succursale en localStorage)
- **Audit Bootstrap × header** : aucun reactstrap/Bootstrap dans les
  composants header ; dépendance SCSS (breakpoints,
  `$container-max-widths`, etc.) documentée pour plus tard

### Body — première passe (working tree)
- **Décision d'architecture** : tranché en faveur de l'**App Router**
  (Next.js 15) — Pages Router écarté définitivement ; toutes les nouvelles
  routes vivent sous `src/app/`
- **Routes e-commerce montées** :
  `/shop`, `/shop/[slug]`, `/produit/[slug]`, `/panier`, `/panier/commande/[token]`,
  `/compte` (layout + stubs), `/login`
- **Composants clients créés** : `ShopListingInitializer.tsx`,
  `ShopCategoryPageClient.tsx`, `ProductPageClient.tsx`,
  `PanierPageContent.tsx`
- **Migration `next/router` → `next/navigation`** :
  `ShopPageShop.tsx` (`useSearchParams` + `usePathname` + `useRouter`),
  `AccountLayout.tsx` (`usePathname`) — `'use client'` ajouté sur les deux
- **Bugs silencieux corrigés** : classes `d-none d-lg-block` / `d-block`
  remplacées par `hidden lg:block` / `block` (Tailwind) dans
  `ShopSidebar.tsx` et `CheckoutForm.tsx`
- **Dropdown langue** : `panelVisibility` manquant ajouté au `className`
  du panneau — le menu se ferme maintenant correctement
- **`url.ts` + `router.ts`** : chemins alignés sur les nouvelles routes
  App Router (`/shop`, `/produit/...`, `/panier`, `/login`, `/compte/...`)
- **Build vert** : `npx next build --no-lint` passe en exit 0 sur
  l'ensemble des modifications

### Automatisation IA
- Tâches de migration répétitives (remplacement `next/router`, fix classes
  Bootstrap, création des routes) déléguées à Cursor — focus personnel
  maintenu sur le comportement attendu, la cohérence des décisions
  d'architecture et la validation des diffs avant merge
- Gain de temps estimé : plusieurs heures sur des tâches mécaniques à
  faible valeur ajoutée

---

## ⏸️ Hors scope — volontairement pas traité

- **Migration Redux shop → Zustand** : store shop intact jusqu'au
  branchement API Go — une seule migration au lieu de deux
- **Fake API** : reste en place pour la phase UI ; remplacement par
  l'API Go prévu en phase suivante
- **Migration SCSS → Tailwind** : chantier distinct, après stabilisation
  de l'UI
- **Fiches succursales** : renvoi vers `midbec.com` pour l'instant
- **Adresses complètes** : à enrichir quand les sources sont figées

---

## 🔜 Demain

- Remplacer les slides RedParts par les assets Midbec (`BlockSlideshow`)
- Continuer la QA visuelle des parcours montés aujourd'hui
  (home → catalogue → fiche produit → panier, login → compte)
- Commit groupé du working tree avec message structuré
- Si besoin : enchaîner sur les blocs home (BlockFeatures, BlockBanners,
  BlockCategories)