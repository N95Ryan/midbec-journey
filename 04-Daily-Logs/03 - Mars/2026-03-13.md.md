# 📅 Vendredi 13 mars 2026 — Fin de ma première semaine à Midbec

**Contexte** : Je suis arrivé plus léger qu'hier. J'ai mieux dormi, fait de la self affirmation, bu un smoothie protéiné : je suis prêt à itérer aujourd'hui.

Petite chose intéressante de la journée : Patrick m'a proposé de venir déjeuner tous les vendredis avec un ami à lui qui est SFE à Toronto. C'est bon pour le projet North Star.

---

## ✅ Ce que j'ai fait

- Accepté la proposition de Patrick : déjeuner tous les vendredis avec son ami SFE à Toronto (bon pour le North Star).

### Côté technique

- **Migration FormattedMessage → intl.formatMessage** : Remplacement de tous les usages JSX `<FormattedMessage />` par `intl.formatMessage({ id: '...' })` dans tout le projet pour compatibilité React 19 (erreur de type « cannot be used as a JSX component »). Aucune occurrence restante dans `Sources/src`.
- **Correction du type `containerRef`** dans le context mobile menu (`services/mobile-menu.ts`) : `RefObject<HTMLElement | null> | null` pour aligner avec `useRef<HTMLDivElement>(null)`.
- **Tentatives sur le build** : Ajout d’un retour typé `React.ReactNode` sur `renderPanel` puis cast `as unknown as React.ReactNode` sur le retour de `ReactDOM.createPortal` dans `MobileMenuPanelController.tsx` — le build échoue encore (conflit entre deux copies de `@types/react`). Problème isolé et décomposé en to-do pour lundi.

---

## 💡 Ce que j'ai appris

### Côté technique

- En React 19, les types JSX et les refs sont plus stricts ; `FormattedMessage` (composant de `react-intl`) n’est plus reconnu comme composant JSX valide, d’où la migration vers `useIntl()` + `formatMessage()`.
- Un projet peut avoir plusieurs copies de `@types/react` (racine + sous-dépendance de `@types/react-dom`), ce qui provoque des erreurs « Type X is not assignable to type X » entre `ReactPortal` et `ReactNode`.

---

## ⚠️ Points de vigilance

### Côté technique

- Le build ne passe toujours pas à cause de `MobileMenuPanelController.tsx` (ligne 56, type `ReactPortal` vs `ReactNode`). À traiter en priorité lundi.
- Vérifier après correction du build qu’aucune régression sur le menu mobile (portals, navigation entre panneaux).

---

## 📋 Semaine prochaine

- [ ] **Lundi – Build / ReactPortal** : exécuter la to-do list ci-dessous pour débloquer le build.
- [ ] Auditer les dépendances legacy encore présentes dans `package.json`
- [ ] Décider lesquelles peuvent être supprimées immédiatement et lesquelles doivent rester temporairement
- [ ] Continuer la sortie progressive de `Bootstrap` / `Sass` au profit de `Tailwind`
- [ ] Cibler un lot UI réel visible à nettoyer en priorité : header, search, footer ou product-card
- [ ] Recenser les occurrences restantes de `RedParts` hors logo
- [ ] Reprendre le bug API une fois le périmètre technique du front stabilisé

---

## 🔧 To-do Lundi — Isoler et résoudre l’erreur de build (ReactPortal)

**Erreur actuelle** :
```text
./src/components/mobile/MobileMenuPanelController.tsx:56:9
Type error: Type 'ReactPortal' is not assignable to type 'ReactNode'.
  (ReactPortal de @types/react-dom/node_modules/@types/react ≠ React.ReactPortal du @types/react racine)
```

**Blocs à enchaîner** :

| # | Tâche | Détail |
|---|--------|--------|
| 1 | **Confirmer la duplication** | Vérifier la présence de `node_modules/@types/react-dom/node_modules/@types/react` et comparer les versions avec `node_modules/@types/react`. |
| 2 | **Option A — Résolutions** | Si duplication confirmée : ajouter `overrides` (npm) ou `resolutions` (bun) dans `package.json` pour forcer une seule version de `@types/react` partout. Puis `bun install` et `bun run build`. |
| 3 | **Option B — Wrapper createPortal** | Créer une fonction du type `renderPortal(children, container): React.ReactNode` qui appelle `ReactDOM.createPortal(children, container)` et retourne le résultat casté en `React.ReactNode`. Utiliser cette fonction dans `MobileMenuPanelController.tsx` à la place de l’appel direct. |
| 4 | **Option C — Cast / typage local** | Si A et B ne suffisent pas : cast plus ciblé ou fichier `.d.ts` local pour unifier les types de portal. |
| 5 | **Valider** | Lancer `cd Sources && bun run build` et confirmer que la compilation passe. |

**Fichiers concernés** :  
`Sources/src/components/mobile/MobileMenuPanelController.tsx` (ligne 56), `Sources/package.json` (si Option A).

---

**Énergie** : 7/10 — C'était un bon début de mission, j'ai setup mon environnement et je me suis organisé.
